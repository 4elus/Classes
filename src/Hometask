Задание:

Создать класс Animal у которого будут поля
-name
-age
-color
-fed(сытый)

для каждого поля должден быть установлен геттер и сеттер
также должен быть создан конструктор для инициализации полей объекта.

создать метод run, который воводит сообщение в консоль о том, что
animal.name пробежал столько(значение) метров.
